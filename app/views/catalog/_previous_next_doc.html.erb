<% #Using the Bootstrap Pagination class  -%>
<% if @previous_document || @next_document %>
	<div id="previousNextDocument">
    <div class="page_links">
      <% if @previous_document  %>
        <%= link_to_previous_document @previous_document %> |
      <% end %>
      <%= item_page_entry_info %> 
      <% if @next_document  %> 
        | <%= link_to_next_document @next_document %>
      <% end %>
    </div>

    <div class="pull-right search-widgets">
        <%= link_back_to_catalog :class => 'btn btn-ltblue' %>
        
        <%=link_to "#{t('blacklight.search.start_over')}", start_over_path(current_search_session.try(:query_params) || {}), :id=>"startOverLink", :class => 'btn btn-ltblue' %>
  	</div>
  </div>
<% end %>
