Description:
    Creates a new database migration from the symphony config data dump found at
    `data/data4discovery`
    The migration class is generated in db/migrate prefixed by a timestamp of the
    current date and time.

Example:
    `rails generate symphony_nightly`

    If the current date is May 14, 2008 and the current time 09:09:12, this
    creates the SymphonyNightly migration
      db/migrate/20080514090912_symphony_nightly_20080514.rb
